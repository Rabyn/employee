"USE STRICT"

var myApp = angular.module("appEmp", ["ui.bootstrap.modal"]);

/*var myApp = angular.module("appEmp", ["ngroute", "ui.bootstrap.modal"]);
myApp.config(function ($locationProvider, $routeProvider) {
    $routeProvider
        .when('/', {
            templateUrl: "/templates/samplePerson.html"
        }).when('/forms', {
            templateUrl: "/templates/personForm.html"
        }).when('/', {
            templateUrl: "/templates/error.html"
        })

});*/
